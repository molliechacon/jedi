{
  // needed for heroku
  "name": "starwars-express",
  "version": "1.0.0",
  "description": "",
  // needed for heroku
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.15.0",
    "express": "^4.13.4"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    // needed for heroku
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "6.11.0"
  }
}
